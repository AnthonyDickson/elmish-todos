[general]
connection = "Data Source=server/ElmishTodos.Server/todos.db"
output = "Db.fs"
namespace = "ElmishTodos.Server.Db"
cli_mutable = true

[sqlhydra_query_integration]
provider_db_type_attributes = true
table_declarations = true

[filters]
include = []
exclude = []
